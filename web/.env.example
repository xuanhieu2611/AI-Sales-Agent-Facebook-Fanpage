# ── Lead form → Google Sheet ─────────────────────────────────────────
# URL Web App của Apps Script. Xem apps-script/leads/README.md để dựng.
# Nếu để trống: form vẫn chạy, lead chỉ ghi ra log server (dùng khi test).
LEAD_WEBHOOK_URL=

# Chuỗi bí mật, phải trùng với hằng SECRET trong apps-script/leads/Code.gs
LEAD_WEBHOOK_SECRET=
